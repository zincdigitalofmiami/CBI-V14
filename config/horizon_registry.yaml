# ⚠️ CRITICAL: NO FAKE DATA ⚠️
# This project uses ONLY real, verified data sources. NO placeholders, NO synthetic data, NO fake values.
# All data must come from authenticated APIs, official sources, or validated historical records.
#

version: 1
updated: 2025-11-18
notes: |
  Machine-readable registry for training horizons. Scripts should consume this
  instead of hardcoding horizon sets. "active" are in production focus; 
  "research" are special tracks gated by validation before promotion.

assets:
  MES:
    granularity:
      intraday: "1m bars"
      main: "daily bars"
    model_types:
      special: "neural (LSTM/TCN/CNN-LSTM)"
      main: "trees (LightGBM/XGBoost)"
    # All horizons (prod + allhistory)
    horizons:
      intraday: ["1min", "5min", "15min", "30min", "1hr", "4hr"]
      daily: ["1d", "7d", "30d"]
      monthly: ["3m", "6m", "12m"]
    intraday_special:
      active: ["5min", "15min", "1hr"]
      research: ["1min", "30min", "4hr"]
    main:
      active: ["1d", "7d", "30d"]
      research: ["3m", "6m", "12m"]
  ZL:
    granularity: "daily bars"
    model_types:
      main: "trees (LightGBM/XGBoost), optional neural"
    main:
      active: ["1w", "1m", "3m", "6m", "12m"]
